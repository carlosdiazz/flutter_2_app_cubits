name: flutter_2_forms_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^10.1.0 #Manejo de la ruta
  flutter_bloc: ^8.1.3 #Gestor de estado
  equatable: ^2.0.5 #Comparar Objecto
  formz: ^0.6.0 #Validar Formualrio

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
